manifest_version: 1
# Legacy-stable runtime identity: changing this would break existing Hermes
# install directories, config keys, and update/remove commands.
name: telegram-business-voice-transcriber
version: 0.6.2
description: "Update-persistent Hermes Telegram Business integration with voice and video-note transcription, configurable transcript enrichment, and Business-scoped replies."
author: neoromantic
kind: standalone
provides_hooks:
  - pre_gateway_dispatch
